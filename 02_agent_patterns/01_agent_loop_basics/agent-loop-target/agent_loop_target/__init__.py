"""agent_loop_target — five small, self-contained features, each shipped with a bug.

Every module's public function has a docstring describing the *intended* behavior.
The matching test in ``tests/`` encodes that behavior and currently fails. Fixing
the implementation so the test passes is the task.
"""
